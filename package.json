{
  "dependencies": {
    "dotenv": "^17.2.3"
  },
  "name": "mini-vercel",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf build && mv frontend/out build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
